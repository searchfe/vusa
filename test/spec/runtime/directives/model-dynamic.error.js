import Vue from '../../../helpers/vue';

describe('Directive a-model dynamic input type', () => {
    // it('should warn', function () {
    //     new Vue({
    //         data: {
    //             type: 'text',
    //             text: 'hi',
    //         },
    //         template: '<input :type="type" a-model="text">',
    //     }).$mount();
    //     expect('a-model does not support dynamic input types').toHaveBeenWarned();
    // });
});
