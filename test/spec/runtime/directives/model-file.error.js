import Vue from '../../../helpers/vue';

describe('Directive a-model file', () => {
    // it('warn to use @change instead', () => {
    //     new Vue({
    //         data: {
    //             file: '',
    //         },
    //         template: '<input a-model="file" type="file">',
    //     }).$mount();
    //     expect('Use a a-on:change listener instead').toHaveBeenWarned();
    // });
});
